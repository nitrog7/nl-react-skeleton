import React from 'react';

export class DemoButton extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <button>Demo</button>
    );
  }
}

export default DemoButton;
